=== НАЧАЛО КОНТЕКСТА ДЛЯ ИИ ===
ПРОЕКТ: gonchaya (https://github.com/Alexander-Firsov/gonchaya). Модуль в стадии активной разработки.

1.  ГЛОБАЛЬНЫЕ ПРАВИЛА СТИЛЯ
    •   ПРАВИЛО 1: Использовать современный синтаксис аннотаций типов (Python 3.10+).
        ПРИМЕР: `def func(arg: str | None) -> _BashOut:` (ДА).
        ПРИМЕР: `def func(arg: Optional[str]) -> _BashOut:` (НЕТ).
    •   ПРАВИЛО 2: Комментарии с двойной решеткой `##` не удалять и не изменять никогда.
        КОНТЕКСТ: Это специальные метки-якоря. Содержимое после `##` неприкосновенно. **Это правило имеет наивысший приоритет.**
    •   ПРАВИЛО 3: Type hints обязательны для всех публичных функций.
    •   ПРАВИЛО 4: Все комментарии в замороженных функциях сохранять как якоря логики.
        ИНТЕРПРЕТАЦИЯ ДЛЯ ИИ: В функциях с `@freeze_permanently` комментарии (особенно `##`) критически важны. Не перефразировать, не удалять, не перемещать. Даже если содержимое кажется некорректным, это "хлебные крошки" для разработчика.
    •   ПРАВИЛО 5: Не предлагать перевод docstring, комментариев или именований на английский без явного указания.
        КОНТЕКСТ: Все разработчики русскоязычные. Перевод замедлит разработку.
        ПРИМЕР: Допустим docstring: `"""Выполняет команду в shell."""`

2.  КЛЮЧЕВОЙ КОНТЕКСТ ПРОЕКТА
    •   СИСТЕМА РЕФАКТОРИНГА: Определена в `src/gonchaya/refactor_phases.py`.
        НАЗНАЧЕНИЕ: Декораторы задают этап/интенцию рефакторинга для функции.
        ПОЛНЫЙ НАБОР ДЕКОРАТОРОВ (согласно коду):
        - `@freeze_permanently()`: Функция заморожена. **Допустимо только указание на явные грамматические/пунктуационные ошибки в тексте комментариев в рамках ответа ИИ. Автоматические правки в код запрещены.** Комментарии после '##' защищены Правилом 2.
        - `@skip_for_now()`: Функцию не трогать в текущем цикле разработки (есть более приоритетные задачи). Изменения не вносить.
        - `@cosmetic_only()`: Только косметические правки (стиль, имена, докстринги). **Логику не менять.**
        - `@allow_logic_change()`: разрешение на изменение бизнес-логики функции.
        - `@allow_for_restructure()`: Функцию можно реструктуризировать (объединять/разделять блоки).
        - `@allow_for_optimization()`: Можно менять алгоритмы для оптимизации производительности.
        - `@allow_architectural()`: Разрешить архитектурные изменения (вынос логики, изменение интерфейса).
        - `@preserve_all_comments_as_anchors(reason: str)`: **ОБЯЗАТЕЛЬНО** сохранить ВСЕ существующие комментарии в функции как якоря для понимания логики. Параметр `reason` объясняет причину.
        •   КАК ИСПОЛЬЗОВАТЬ С ИИ: При ревью или запросе на правки функции, ОБЯЗАТЕЛЬНО уточните, какой декоратор к ней применён. Если декоратора нет, по умолчанию считайте, что действует `@cosmetic_only()`.

3.  КАК РАБОТАТЬ С ЭТИМ ФАЙЛОМ (ДЛЯ РАЗРАБОТЧИКА)
    •   Перед началом диалога с ИИ, связанного с кодом, скопируй нужные секции в чат.
    •   Для ревью функции, обёрнутой в декоратор, ОБЯЗАТЕЛЬНО скопируй пункт 2.1 и укажи, какой именно декоратор используется.
    •   Пример начала запроса с контекстом:
        "Используй правила 1.1, 1.4, 1.5. Функция обёрнута в `@freeze_permanently()`. Проверь, нет ли случайных изменений в комментариях: ..."

4.  ШАБЛОНЫ АНАЛИЗА ДЛЯ ИИ
    •   СТРУКТУРА №1: "Технический конспект модуля"
        НАЗНАЧЕНИЕ: Создать исчерпывающий обзор модуля для сохранения контекста между сессиями.
        ФОРМАТ ВЫВОДА: Markdown-документ (`.md`).
        СТРУКТУРА КОНСПЕКТА:
        1.  **Назначение и архитектура** — Кратко, что делает модуль и его основная идея.
        2.  **Ключевые файлы и их роль** — Таблица или список с описанием главных файлов.
        3.  **Критические особенности и правила** — Важные детали реализации, соглашения, предупреждения.
        4.  **Текущий статус разработки** — Что готово, что в работе, что запланировано (с использованием ✅⚙️🚧).
        5.  **Ссылки и контекст** — Пути к ключевым файлам, ссылка на репозиторий.
=== КОНЕЦ КОНТЕКСТА ДЛЯ ИИ ===
    •   КАК ИСПОЛЬЗОВАТЬ:
        - В запросе укажите: "Проанализируй модуль [ИМЯ] и создай файл-конспект, используя **СТРУКТУРУ №1**".
        - Приложите исходный код или ссылку на него.

Пример запроса:
    •   Проанализируй модуль `gonchaya` (исходный код: https://github.com/Alexander-Firsov/gonchaya) и создай файл-конспект, используя **СТРУКТУРУ №1**.
